.DS_Store

# Credential files must never be tracked here — this repo is public.
# Secrets belong in Doppler (see Brewfile); machine-local credential files
# stay untracked and are recreated by hand per machine.
.netrc
**/.netrc
.env
.env.*
*.pem
*.key
id_rsa
id_ed25519
